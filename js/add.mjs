import { saveCard } from './storage.mjs';

document.addEventListener('DOMContentLoaded', ()=>{
  // Placeholder: build add UI here
  console.log('add.mjs loaded');
  // Example: hook a form to saveCard()
});
